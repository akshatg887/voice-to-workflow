import { Inngest } from 'inngest';

/**
 * Inngest client for workflow orchestration
 */
export const inngest = new Inngest({ 
  id: 'voicegraph-workflows',
  name: 'VoiceGraph Workflow Orchestrator'
});

